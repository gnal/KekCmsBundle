{% extends 'MsiAdminBundle:Admin:index.html.twig' %}

{% block sidebar %}
<ul id="sortable1" class="list-group" data-url="{{ path('msi_cms_menunode_sort') }}">
    {% for item in pager.getIterator().getArrayCopy() %}
        {% set l = loop.index %}
        <li class="list-group-item" id="{{ item.id }}" data-lvl="{{ item.lvl }}" data-lft="{{ item.lft }}" data-rgt="{{ item.rgt }}">
            {% for i in 0..item.lvl %}
                {% if i > 1 %}
                    {#{% if item.lvl and i < item.lvl %}
                        {% if i < item.lvl and i > 1 %}
                            <i class="fa fa-plus"></i>
                        {% else %}
                            <i class="fa fa-plus"></i>
                        {% endif %}
                    {% else %}
                        {% if item.rgt == item.parent.rgt - 1 %}
                            <i class="fa fa-minus"></i>
                        {% else %}
                            <i class="fa fa-minus"></i>
                        {% endif %}
                    {% endif %}#}
                    -
                {% endif %}
            {% endfor %}
            {{ item.translation.name|truncate(9, false, '...') }}
        </li>
    {% endfor %}
</ul>
{% endblock sidebar %}

{% block js %}
{{ parent() }}
<script src="{{ asset('bundles/msiadmin/js/tree.js') }}"></script>
{% endblock js %}
